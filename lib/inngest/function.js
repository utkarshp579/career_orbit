// demo how function works , help of ingest
import { inngest } from "./client";

// demo purpose
// export const helloWorld = inngest.createFunction(
//   { id: "hello-world" }, // other details could be added here inside id's object
//   { event: "test/hello.world" },
//   async ({ event, step }) => {
//     await step.sleep("wait-a-moment", "1s");
//     return { message: `Hello ${event.data.email}!` };
//   }
// );

// Creating a function Which will fetch Questions from Gemini API 
 